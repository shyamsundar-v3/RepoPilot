# Backend API base URL (FastAPI, from `backend/run.py`)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Backend WebSocket base URL — used for the live agent trace stream
NEXT_PUBLIC_WS_URL=ws://localhost:8000
